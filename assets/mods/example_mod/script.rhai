// Return a map that describes a time-based effect
// Keys: start (YYYY-MM-DD string), months (i64), cost_pct (f32), yield_delta (f32)

#{
    start: "1998-01-01",
    months: 6,
    cost_pct: 15.0,
    yield_delta: -0.02
}

